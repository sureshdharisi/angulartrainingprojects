import { CustometemperaturPipe } from './custometemperatur.pipe';

describe('CustometemperaturPipe', () => {
  it('create an instance', () => {
    const pipe = new CustometemperaturPipe();
    expect(pipe).toBeTruthy();
  });
});
